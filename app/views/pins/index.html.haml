#pins.transitions-enabled
    - @pins.each do |pin|
        .box.panel.panel-default
            = link_to image_tag(pin.image.url), pins_path
            .panel-body
                %h2= link_to pin.title, pins_path
                %p.user
                Submitted by
                = pin.user.email    
